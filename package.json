{
  "name": "forward_bot",
  "description": "Forwards bot from a channel to group",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run api/app.ts",
    "dev": "bun --watch api/app.ts"
  },
  "dependencies": {
    "express": "^4.19.2",
    "grammy": "^1.28.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
